export VENDOR_DIR="vendor"
export DUCKDB_VERSION="1.0.0"
# Set GEM_HOME and BUNDLE_PATH to the same directory
export GEM_HOME=$BUILD_DIR/$VENDOR_DIR
export BUNDLE_PATH=$GEM_HOME
export DUCKDB_DIR=$BUILD_DIR/$VENDOR_DIR
export DUCKDB_LIB=$DUCKDB_DIR/lib
export DUCKDB_INCLUDE=$DUCKDB_DIR/include
